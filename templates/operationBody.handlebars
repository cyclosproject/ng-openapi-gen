{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): __Observable<{{{resultType}}}> {

    return this.{{responseMethodName}}(params).pipe(
      __map((r: {{{httpResponseType}}}) => r.body as {{{resultType}}})
    );
  }
