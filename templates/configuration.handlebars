/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { Injectable } from '@angular/core';

/**
 * Provides the {{configurationClass}} configuration object with a given root URL.
 */
export function provide{{configurationClass}}(rootUrl: string) {
  var config = new {{configurationClass}}();
  config.rootUrl = rootUrl;
  return {
    provide: {{configurationClass}},
    useValue: config
  };
}

/**
 * Global configuration
 */
@Injectable({
  providedIn: 'root',
})
export class {{configurationClass}} {
  rootUrl: string = '{{{rootUrl}}}';
}

{{#if moduleClass}}
/**
 * Parameters for `{{moduleClass}}.forRoot()`
 */
export interface {{configurationParams}} {
  rootUrl?: string;
}
{{/if}}
